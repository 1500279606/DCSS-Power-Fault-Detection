# Datasets

Due to dataset licenses and access restrictions, datasets are not included in
this repository. Please obtain the datasets from their official sources.

The public code expects an external `.npz` file containing `train`, `test`, and
optionally `test_labels`. Dataset files should be stored outside the repository
or under the ignored `data/` directory.

